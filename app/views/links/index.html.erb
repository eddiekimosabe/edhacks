<div class="link_list">
  <% @links.each do |l| %>

    <p>
    	<a href=<%= l.id %> > <%= l.language %>
    		<%= l.title %>:  <%= l.url %><br> [<%= l.vote_count %>]
    	</a>
    </p>

  <% end %>
<div>